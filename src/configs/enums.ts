enum enums { 
	// GLOBAL_ENV = 'GLOBAL_ENV'
}

export enum Genders {
	"Nam",
	"Nữ", 
	"Khác" 
}

export enum ResearchTeamTitles {
	'Thành viên',
	'Nhóm trưởng'
}

export enum DeviceType {
	"Thiết bị" = 0,
	"Công cụ" = 1,
	"Dụng cụ" = 2,
}

export default enums;